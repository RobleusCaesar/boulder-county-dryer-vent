# Stripe stubs only — this static export does not charge cards.
# Do not commit live keys. Wire Payment Element / Checkout against a small API later.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
