{
  "name": "boulder-county-dryer-vent",
  "version": "2.0.0",
  "private": true,
  "description": "Boulder County Dryer Vent — public site and booking funnel. Static HTML, no dependencies.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "npm run build && python -m http.server 8787 -d dist"
  },
  "engines": {
    "node": ">=20"
  }
}
